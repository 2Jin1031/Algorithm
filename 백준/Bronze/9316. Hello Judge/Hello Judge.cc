#include <iostream>
#include <locale>

using namespace std;

int main(void) {
    int N; cin >> N;
    for (int i = 1; i <= N; i++) {
        cout << "Hello World, Judge " << i << "!" << endl;
    }
}