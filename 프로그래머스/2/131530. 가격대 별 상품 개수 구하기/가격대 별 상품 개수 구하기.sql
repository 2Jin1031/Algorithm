SELECT CASE WHEN PRICE / 10000 < 1 THEN 0 ELSE ((FLOOR(PRICE / 10000)) * 10000) END AS PRICE_GROUP, COUNT (DISTINCT PRODUCT_ID) AS PRODUCTS
FROM PRODUCT
GROUP BY ((FLOOR(PRICE / 10000)) * 10000)
ORDER BY PRICE_GROUP